{
  "scripts": {
    "build:css": "concat-cli -f styles/variables/*.css styles/base/*.css styles/layout/*.css styles/components/*.css styles/utilities/*.css styles/custom.css -o styles/vanilla-combined.css",
    "minify:css": "cleancss -o styles/vanilla-combined.min.css styles/vanilla-combined.css && node scripts/postprocess-css.js",
    "switch:prod": "node scripts/switch-mode.js prod",
    "switch:dev": "node scripts/switch-mode.js dev",
    "css-to-tokens": "node scripts/css-to-tokens.js",
    "tokens-to-css": "node scripts/tokens-to-css.js",
    "build": "npm run build:css && npm run minify:css && npm run switch:prod",
    "dev": "npm run switch:dev"
  },
  "devDependencies": {
    "postcss": "^8.5.1",
    "postcss-js": "^4.0.1",
    "clean-css-cli": "^5.6.3",
    "concat-cli": "^4.0.0"
  }
}
