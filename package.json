{
  "scripts": {
    "build:css": "concat-cli -f styles/base/reset.css styles/variables/*.css styles/base/typography.css styles/layout/grid.css styles/components/*.css styles/utilities/*.css styles/custom.css -o styles/vanilla-combined.css",
    "minify:css": "cleancss -o styles/vanilla-combined.min.css styles/vanilla-combined.css",
    "switch:prod": "node scripts/switch-mode.js prod",
    "switch:dev": "node scripts/switch-mode.js dev",
    "build": "npm run build:css && npm run minify:css && npm run switch:prod",
    "dev": "npm run switch:dev"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "concat-cli": "^4.0.0"
  }
}